import { CreateRecipeSchemaData } from "@/schemas/recipe-schemas";

export default async function createRecipe(data: CreateRecipeSchemaData) {
    console.log("laten we het maken!");

    // TODO: verifier eerst nog iets?? Als het schema klopt is het allemaal prima toch??
}